---
// Chrome bersama 3 halaman Starter Pack, sumber Figma node 796:40.
import SectionTitle from './SectionTitle.astro';

interface Props {
	active: 'dresscode' | 'peralatan' | 'dos-donts';
}

const { active } = Astro.props;

const tabs = [
	{ key: 'dresscode', label: 'Dress code', href: '/info-gensoed/starterpack/dresscode' },
	{ key: 'peralatan', label: 'Barang bawaan', href: '/info-gensoed/starterpack/peralatan' },
	{ key: 'dos-donts', label: "Do's & Don'ts", href: '/info-gensoed/starterpack/dos-donts' },
] as const;
---

<section class="relative overflow-hidden bg-[#76a3ed] pt-16 pb-10 md:pt-24">
	<div class="pointer-events-none absolute inset-0 z-0" aria-hidden="true">
		<img src="/images/elements/bgbatik.svg" alt="" class="h-full w-full object-cover opacity-10" />
	</div>

	<img
		src="/images/elements/awan-biru.svg"
		alt=""
		class="pointer-events-none absolute top-24 left-0 z-0 hidden w-40 md:block lg:w-56"
	/>
	<img
		src="/images/elements/awan-biru.svg"
		alt=""
		class="pointer-events-none absolute top-24 right-0 z-0 hidden w-40 -scale-x-100 md:block lg:w-56"
	/>

	<div class="relative z-10 mx-auto flex max-w-screen-xl flex-col items-center px-4 text-center">
		<SectionTitle title="Starter Pack" as="h1" />

		<p class="font-reikna text-white text-lg sm:text-2xl md:text-3xl max-w-3xl" data-aos="fade-up" data-aos-delay="100">
			Panduan lengkap untuk mempersiapkan diri mengikuti Soedirman Student Summit <span class="font-miskan text-sm sm:text-base md:text-lg">2026</span>
		</p>

		<nav
			aria-label="Navigasi Starter Pack"
			class="mt-8 flex w-full max-w-4xl flex-wrap justify-center gap-3"
			data-aos="fade-up"
			data-aos-delay="200"
		>
			{tabs.map((tab) => (
				<a
					href={tab.href}
					class:list={[
						'font-reikna flex w-full max-w-[316px] items-center justify-center rounded-[10px] px-4 py-3 text-center text-lg whitespace-nowrap text-[#ffc03f] transition-colors sm:w-auto sm:flex-1 sm:text-2xl md:text-[48px]',
						active === tab.key ? 'bg-[#1d3464]' : 'bg-[rgba(255,255,255,0.25)] hover:bg-[rgba(255,255,255,0.4)]',
					]}
					aria-current={active === tab.key ? 'page' : undefined}
				>
					{tab.label}
				</a>
			))}
		</nav>
	</div>
</section>
